{
  "name": "socket-chat-example",
  "version": "0.0.1",
  "description": "my first socket.io app",
  "scripts": {
    "server": "nodemon server.js",
    "client": "browser-sync start --no-ghost-mode --server --files \"*.css, *.html, scripts/*.js\""
  },
  "dependencies": {
    "express": "^4.17.1",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "eslint": "^6.6.0",
    "nodemon": "^1.19.4"
  }
}
